language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
script:
  - npm run build-storybook
deploy:
    provider: pages
    cleanup: true
    token: $github_token
    local_dir: storybook_static
    on:
        branch: test1
    